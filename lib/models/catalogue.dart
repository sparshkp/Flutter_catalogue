class Item{
  final String id;
  final String description;
  final String name;
  final String color;
  final String Imageurl;
  final num price;

  Item(this.id, this.description, this.name, this.color, this.Imageurl, this.price);
  final products =[Item(
    id, 
    description, 
    name, 
    color, 
    Imageurl,
     price)];
}